#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

gem install --user --no-document bundler:2.0.1
bundle update --bundler
bundle install --binstubs=.bundle/bin

# Do any other automated setup that you need to do here
