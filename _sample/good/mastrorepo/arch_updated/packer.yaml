version: [0, 0, 1]
type: vm
import:
  - arch_base
packer_yaml:
  builders:
    - type: vagrant
      source: '{{ user `mastoras.artifacts.arch_base.box` }}'
  provisioners:
    - type: shell
      inline: |
        echo sudo pacman -Syu
