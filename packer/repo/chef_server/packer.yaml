---
builders:
  - type: vagrant
    communicator: ssh
provisioners:
  - type: shell
    inline: |
      sudo hostnamectl set-hostname server.sp.org &&
      bash -x /local/install-chef-server.bash -- -
