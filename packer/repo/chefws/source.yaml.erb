% vagrant = rebind 'ubuntu-18.04'
name: <%= vagrant.metadata['name'] %>
versions:
  - version: <%= vagrant.build_id %>
    providers:
      - name: virtualbox
        url: <%= vagrant.metadata_json %>
